#include <stdio.h>
#include <stdlib.h>
#include <string.h>


char* twoSumPairs(int *arr, int n, int target) {

    //Enter logic here.
    char* result = (char*)malloc(sizeof(char) * 1);
    if (result != NULL) {
        result[0] = '\0'; 
    }
    return result;
}